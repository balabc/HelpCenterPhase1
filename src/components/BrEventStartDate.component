<apex:component id="EventStartDate" controller="BrEventStartDateCtrl" access="global">
    <apex:attribute name="startDateValue" type="Datetime" assignTo="{!startDate}" description="Start event date for the email template"/>
    <apex:attribute name="timezoneValue" type="String" assignTo="{!timezone}" description="Timezone for event start date"/>
    <apex:attribute name="localeValue" type="String" assignTo="{!locale}" description="Locale for event"/>
    <apex:outputText value="{!startDateFormatted}" />
</apex:component>